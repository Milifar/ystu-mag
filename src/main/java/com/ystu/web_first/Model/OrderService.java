package com.ystu.web_first.Model;

public class OrderService {
    public void createOrder(Order order) {
        //Customer user = SecurityContextHolder.getLoggedUser();
        //checkEligibility(user, order.getSum());
        //order.setCustomer_id(user.getId());

    }
}
